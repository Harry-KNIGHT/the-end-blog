

<div class="row">
    <div class="col-xs-offset-2 col-xs10">
        <h2><%= @article.title%><h2>
        <div class="well col-xs-6">
            <%= @article.description %>
        <% if @article.user %>
        <div class="articles-actions">
            <%= gravatar_for @article.user, size: 20%> <br/>
            <small>Article crée par <%= link_to @article.user.username, user_path(@article_user) %></small>
        </div> 
        <% end %>
                <div class="articles-actions">
                <%= link_to "Edit this article", edit_article_path(@article), class:"btn btn-xs btn-primary" %>
                <%= link_to "Delet this article", article_path(@article), method: :delete, class:"btn btn-xs btn-danger" %>
                <%= link_to "Articles", articles_path, class:"btn btn-xs btn-success" %> 
            </div>
        </div>
    </div>
</div>

</div>